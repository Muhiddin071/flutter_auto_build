name: flutter_auto_build
description: A CLI tool to automate flutter clean, pub get, and build apk/aab with env switching.
version: 1.0.1
homepage: https://github.com/yourusername/flutter_auto_build

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.4.2

dev_dependencies:
  lints: ^3.0.0
